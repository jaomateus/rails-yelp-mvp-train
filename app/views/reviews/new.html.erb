<div class="container mt-5">
  <h1>Add review to <%= @restaurant.name %> restaurant</h1>

    <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit 'Add review', class: 'btn btn-primary' %>
  <% end %>
</div>
